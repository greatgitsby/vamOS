diff --git a/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_cci/cam_cci_core.c b/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_cci/cam_cci_core.c
index 82dff99..44c0c24 100644
--- a/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_cci/cam_cci_core.c
+++ b/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_cci/cam_cci_core.c
@@ -1360,6 +1360,9 @@ static int32_t cam_cci_read(struct v4l2_subdev *sd,
 	val = CCI_I2C_SET_PARAM_CMD | c_ctrl->cci_info->sid << 4 |
 		c_ctrl->cci_info->retries << 16 |
 		c_ctrl->cci_info->id_map << 18;
+	pr_emerg("vamos-cam-cci: read word set-param master=%d queue=%d val=0x%x sid=0x%x retries=%u id_map=%u\n",
+		master, queue, val, c_ctrl->cci_info->sid,
+		c_ctrl->cci_info->retries, c_ctrl->cci_info->id_map);
 	rc = cam_cci_write_i2c_queue(cci_dev, val, master, queue);
 	if (rc < 0) {
 		pr_emerg("vamos-cam-cci: write set-param failed val=0x%x rc=%d\n",
@@ -1370,6 +1373,8 @@ static int32_t cam_cci_read(struct v4l2_subdev *sd,
 	}
 
 	val = CCI_I2C_LOCK_CMD;
+	pr_emerg("vamos-cam-cci: read word lock master=%d queue=%d val=0x%x\n",
+		master, queue, val);
 	rc = cam_cci_write_i2c_queue(cci_dev, val, master, queue);
 	if (rc < 0) {
 		CAM_DBG(CAM_CCI,
@@ -1403,6 +1408,8 @@ static int32_t cam_cci_read(struct v4l2_subdev *sd,
 	}
 
 	val = CCI_I2C_READ_CMD | (read_cfg->num_byte << 4);
+	pr_emerg("vamos-cam-cci: read word read master=%d queue=%d val=0x%x num=%u\n",
+		master, queue, val, read_cfg->num_byte);
 	rc = cam_cci_write_i2c_queue(cci_dev, val, master, queue);
 	if (rc < 0) {
 		CAM_DBG(CAM_CCI,
@@ -1413,6 +1420,8 @@ static int32_t cam_cci_read(struct v4l2_subdev *sd,
 	}
 
 	val = CCI_I2C_UNLOCK_CMD;
+	pr_emerg("vamos-cam-cci: read word unlock master=%d queue=%d val=0x%x\n",
+		master, queue, val);
 	rc = cam_cci_write_i2c_queue(cci_dev, val, master, queue);
 	if (rc < 0) {
 		CAM_DBG(CAM_CCI,
diff --git a/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_cci/cam_cci_dev.c b/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_cci/cam_cci_dev.c
index fb5a39f..a3afe21 100644
--- a/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_cci/cam_cci_dev.c
+++ b/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_cci/cam_cci_dev.c
@@ -163,8 +163,9 @@ irqreturn_t cam_cci_irq(int irq_num, void *data)
 			cci_master_info = &cci_dev->cci_master_info[MASTER_0];
 			cci_dev->cci_master_info[MASTER_0].reset_pending =
 				false;
-			if (!cci_master_info->status)
-				complete(&cci_master_info->reset_complete);
+			complete(&cci_master_info->reset_complete);
+			pr_emerg("vamos-cam-cci: irq reset-done completed master=0 status=%d\n",
+				cci_master_info->status);
 
 			complete_all(&cci_master_info->rd_done);
 			complete_all(&cci_master_info->th_complete);
@@ -173,8 +174,9 @@ irqreturn_t cam_cci_irq(int irq_num, void *data)
 			cci_master_info = &cci_dev->cci_master_info[MASTER_1];
 			cci_dev->cci_master_info[MASTER_1].reset_pending =
 				false;
-			if (!cci_master_info->status)
-				complete(&cci_master_info->reset_complete);
+			complete(&cci_master_info->reset_complete);
+			pr_emerg("vamos-cam-cci: irq reset-done completed master=1 status=%d\n",
+				cci_master_info->status);
 
 			complete_all(&cci_master_info->rd_done);
 			complete_all(&cci_master_info->th_complete);
@@ -364,9 +366,9 @@ irqreturn_t cam_cci_irq(int irq_num, void *data)
 				base, irq_status0);
 
 		cci_dev->cci_master_info[MASTER_0].reset_pending = true;
-		pr_emerg("vamos-cam-cci: irq error master=0 issuing reset status0=0x%x\n",
+		pr_emerg("vamos-cam-cci: irq error master=0 issuing halt status0=0x%x\n",
 			irq_status0);
-		cam_io_w_mb(CCI_M0_RESET_RMSK, base + CCI_RESET_CMD_ADDR);
+		cam_io_w_mb(CCI_M0_HALT_REQ_RMSK, base + CCI_HALT_REQ_ADDR);
 	}
 	if (irq_status0 & CCI_IRQ_STATUS_0_I2C_M1_ERROR_BMSK) {
 		cci_dev->cci_master_info[MASTER_1].status = -EINVAL;
@@ -412,7 +414,7 @@ irqreturn_t cam_cci_irq(int irq_num, void *data)
 		cci_dev->cci_master_info[MASTER_1].reset_pending = true;
-		pr_emerg("vamos-cam-cci: irq error master=1 issuing reset status0=0x%x\n",
+		pr_emerg("vamos-cam-cci: irq error master=1 issuing halt status0=0x%x\n",
 			irq_status0);
-		cam_io_w_mb(CCI_M1_RESET_RMSK, base + CCI_RESET_CMD_ADDR);
+		cam_io_w_mb(CCI_M1_HALT_REQ_RMSK, base + CCI_HALT_REQ_ADDR);
 	}
 
 	return IRQ_HANDLED;
