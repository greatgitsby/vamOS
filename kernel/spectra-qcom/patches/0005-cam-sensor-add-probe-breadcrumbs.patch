diff --git a/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_sensor/cam_sensor_core.c b/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_sensor/cam_sensor_core.c
index 8882b70..f0c6c08 100644
--- a/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_sensor/cam_sensor_core.c
+++ b/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_sensor/cam_sensor_core.c
@@ -5,6 +5,7 @@
  */
 
 #include <linux/module.h>
+#include <linux/gpio.h>
 #include <cam_sensor_cmn_header.h>
 #include "cam_sensor_core.h"
 #include "cam_sensor_util.h"
@@ -384,9 +385,13 @@ static int32_t cam_sensor_update_i2c_info(struct cam_cmd_i2c_info *i2c_info,
 		cci_client->sid = i2c_info->slave_addr >> 1;
 		cci_client->retries = 3;
 		cci_client->id_map = 0;
 		cci_client->i2c_freq_mode = i2c_info->i2c_freq_mode;
+		pr_emerg("vamos-cam-sensor: i2c-info master=%d slave=0x%x sid=0x%x requested_freq=%u active_freq=%u\n",
+			cci_client->cci_i2c_master, i2c_info->slave_addr,
+			cci_client->sid, i2c_info->i2c_freq_mode,
+			cci_client->i2c_freq_mode);
 		CAM_DBG(CAM_SENSOR, " Master: %d sid: 0x%x freq_mode: %d",
 			cci_client->cci_i2c_master, i2c_info->slave_addr,
 			i2c_info->i2c_freq_mode);
 		break;
 
@@ -839,6 +839,10 @@ int32_t cam_sensor_driver_cmd(struct cam_sensor_ctrl_t *s_ctrl,
 	mutex_lock(&(s_ctrl->cam_sensor_mutex));
 	switch (cmd->op_code) {
 	case CAM_SENSOR_PROBE_CMD: {
+		pr_emerg("vamos-cam-sensor: slot %d probe cmd enter handle_type=%d handle=0x%llx\n",
+			s_ctrl->soc_info.index, cmd->handle_type,
+			(unsigned long long)cmd->handle);
+
 		if (s_ctrl->is_probe_succeed == 1) {
 			CAM_ERR(CAM_SENSOR,
 				"Already Sensor Probed in the slot");
@@ -852,6 +856,13 @@ int32_t cam_sensor_driver_cmd(struct cam_sensor_ctrl_t *s_ctrl,
 				CAM_ERR(CAM_SENSOR, "Get Buffer Handle Failed");
 				goto release_mutex;
 			}
+			pr_emerg("vamos-cam-sensor: slot %d parsed probe packet power_up=%u power_down=%u slave=0x%x reg=0x%x expected=0x%x\n",
+				s_ctrl->soc_info.index,
+				power_info->power_setting_size,
+				power_info->power_down_setting_size,
+				s_ctrl->sensordata->slave_info.sensor_slave_addr,
+				s_ctrl->sensordata->slave_info.sensor_id_reg_addr,
+				s_ctrl->sensordata->slave_info.sensor_id);
 		} else {
 			CAM_ERR(CAM_SENSOR, "Invalid Command Type: %d",
 				 cmd->handle_type);
@@ -860,6 +871,8 @@ int32_t cam_sensor_driver_cmd(struct cam_sensor_ctrl_t *s_ctrl,
 		}
 
 		/* Parse and fill vreg params for powerup settings */
+		pr_emerg("vamos-cam-sensor: slot %d fill vreg power-up begin\n",
+			s_ctrl->soc_info.index);
 		rc = msm_camera_fill_vreg_params(
 			&s_ctrl->soc_info,
 			s_ctrl->sensordata->power_info.power_setting,
@@ -870,8 +883,12 @@ int32_t cam_sensor_driver_cmd(struct cam_sensor_ctrl_t *s_ctrl,
 				 rc);
 			goto free_power_settings;
 		}
+		pr_emerg("vamos-cam-sensor: slot %d fill vreg power-up end rc=%d\n",
+			s_ctrl->soc_info.index, rc);
 
 		/* Parse and fill vreg params for powerdown settings*/
+		pr_emerg("vamos-cam-sensor: slot %d fill vreg power-down begin\n",
+			s_ctrl->soc_info.index);
 		rc = msm_camera_fill_vreg_params(
 			&s_ctrl->soc_info,
 			s_ctrl->sensordata->power_info.power_down_setting,
@@ -882,9 +899,15 @@ int32_t cam_sensor_driver_cmd(struct cam_sensor_ctrl_t *s_ctrl,
 				 rc);
 			goto free_power_settings;
 		}
+		pr_emerg("vamos-cam-sensor: slot %d fill vreg power-down end rc=%d\n",
+			s_ctrl->soc_info.index, rc);
 
 		/* Power up and probe sensor */
+		pr_emerg("vamos-cam-sensor: slot %d power_up begin\n",
+			s_ctrl->soc_info.index);
 		rc = cam_sensor_power_up(s_ctrl);
+		pr_emerg("vamos-cam-sensor: slot %d power_up end rc=%d\n",
+			s_ctrl->soc_info.index, rc);
 		if (rc < 0) {
 			CAM_ERR(CAM_SENSOR, "power up failed");
 			goto free_power_settings;
@@ -909,7 +933,57 @@ int32_t cam_sensor_driver_cmd(struct cam_sensor_ctrl_t *s_ctrl,
 
 		/* Match sensor ID */
 		s_ctrl->is_always_on = 0;
+		if (power_info->gpio_num_info) {
+			struct msm_camera_gpio_num_info *gpi =
+				power_info->gpio_num_info;
+			int vana = -1;
+			int reset = -1;
+
+			if (gpi->valid[SENSOR_VANA])
+				vana = gpio_get_value_cansleep(
+					gpi->gpio_num[SENSOR_VANA]);
+			if (gpi->valid[SENSOR_RESET])
+				reset = gpio_get_value_cansleep(
+					gpi->gpio_num[SENSOR_RESET]);
+			pr_emerg("vamos-cam-sensor: slot %d pre-match gpio vana valid=%u gpio=%u val=%d reset valid=%u gpio=%u val=%d\n",
+				s_ctrl->soc_info.index,
+				gpi->valid[SENSOR_VANA],
+				gpi->gpio_num[SENSOR_VANA],
+				vana,
+				gpi->valid[SENSOR_RESET],
+				gpi->gpio_num[SENSOR_RESET],
+				reset);
+			if (gpi->valid[SENSOR_VANA]) {
+				int tlmm_base = gpi->gpio_num[SENSOR_VANA] - 8;
+				int cci_sda = tlmm_base +
+					(s_ctrl->cci_i2c_master ? 19 : 17);
+				int cci_scl = tlmm_base +
+					(s_ctrl->cci_i2c_master ? 20 : 18);
+				int mclk = gpi->valid[SENSOR_MCLK] ?
+					gpio_get_value_cansleep(
+						gpi->gpio_num[SENSOR_MCLK]) : -1;
+				int sda = gpio_get_value_cansleep(cci_sda);
+				int scl = gpio_get_value_cansleep(cci_scl);
+
+				pr_emerg("vamos-cam-sensor: slot %d pre-match bus master=%d mclk valid=%u gpio=%u val=%d cci_sda_gpio=%d val=%d cci_scl_gpio=%d val=%d\n",
+					s_ctrl->soc_info.index,
+					s_ctrl->cci_i2c_master,
+					gpi->valid[SENSOR_MCLK],
+					gpi->gpio_num[SENSOR_MCLK],
+					mclk, cci_sda, sda, cci_scl, scl);
+			}
+		}
+		pr_emerg("vamos-cam-sensor: slot %d powered probe inspection window begin\n",
+			s_ctrl->soc_info.index);
+		msleep(5000);
+		pr_emerg("vamos-cam-sensor: slot %d match_id begin slave=0x%x reg=0x%x expected=0x%x\n",
+			s_ctrl->soc_info.index,
+			s_ctrl->sensordata->slave_info.sensor_slave_addr,
+			s_ctrl->sensordata->slave_info.sensor_id_reg_addr,
+			s_ctrl->sensordata->slave_info.sensor_id);
 		rc = cam_sensor_match_id(s_ctrl);
+		pr_emerg("vamos-cam-sensor: slot %d match_id end rc=%d\n",
+			s_ctrl->soc_info.index, rc);
 		if (rc < 0) {
 			cam_sensor_power_down(s_ctrl);
 			msleep(20);
diff --git a/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_sensor_utils/cam_sensor_util.c b/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_sensor_utils/cam_sensor_util.c
index c48b59d..bf8f3a6 100644
--- a/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_sensor_utils/cam_sensor_util.c
+++ b/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_sensor_utils/cam_sensor_util.c
@@ -1965,6 +1965,12 @@ static int msm_cam_sensor_handle_reg_gpio(int seq_type,
 
 	gpio_offset = seq_type;
 
+	pr_emerg("vamos-cam-sensor: reg-gpio seq=%d valid=%u gpio=%u val=%d\n",
+		seq_type,
+		(gpio_offset >= 0 && gpio_offset < SENSOR_SEQ_TYPE_MAX) ?
+			gpio_num_info->valid[gpio_offset] : 0,
+		(gpio_offset >= 0 && gpio_offset < SENSOR_SEQ_TYPE_MAX) ?
+			gpio_num_info->gpio_num[gpio_offset] : 0, val);
 	if (gpio_num_info->valid[gpio_offset] == 1) {
 		CAM_DBG(CAM_SENSOR, "VALID GPIO offset: %d, seqtype: %d",
 			 gpio_offset, seq_type);
@@ -2109,6 +2109,10 @@ int cam_sensor_core_power_up(struct cam_sensor_power_ctrl_t *ctrl,
 	}
 
 	CAM_DBG(CAM_SENSOR, "power setting size: %d", ctrl->power_setting_size);
+	pr_emerg("vamos-cam-sensor: power_up core dev=%s size=%u num_clk=%u num_rgltr=%d no_gpio=%d pinctrl=%d\n",
+		dev_name(soc_info->dev), ctrl->power_setting_size,
+		soc_info->num_clk, num_vreg, no_gpio,
+		ctrl->cam_pinctrl_status);
 
 	for (index = 0; index < ctrl->power_setting_size; index++) {
 		CAM_DBG(CAM_SENSOR, "index: %d", index);
@@ -2121,9 +2125,15 @@ int cam_sensor_core_power_up(struct cam_sensor_power_ctrl_t *ctrl,
 		}
 
 		CAM_DBG(CAM_SENSOR, "seq_type %d", power_setting->seq_type);
+		pr_emerg("vamos-cam-sensor: power idx=%d seq=%d seq_val=%u config=%ld delay=%u\n",
+			index, power_setting->seq_type, power_setting->seq_val,
+			power_setting->config_val, power_setting->delay);
 
 		switch (power_setting->seq_type) {
 		case SENSOR_MCLK:
+			pr_emerg("vamos-cam-sensor: mclk begin idx=%d seq_val=%u config=%ld num_clk=%u\n",
+				index, power_setting->seq_val,
+				power_setting->config_val, soc_info->num_clk);
 			if (power_setting->seq_val >= soc_info->num_clk) {
 				CAM_ERR(CAM_SENSOR, "clk index %d >= max %u",
 					power_setting->seq_val,
@@ -2169,6 +2179,9 @@ int cam_sensor_core_power_up(struct cam_sensor_power_ctrl_t *ctrl,
 				soc_info->clk_rate[0][power_setting->seq_val] =
 					power_setting->config_val;
 
+			pr_emerg("vamos-cam-sensor: mclk enable clocks count=%u first_rate=%ld\n",
+				soc_info->num_clk,
+				soc_info->num_clk ? soc_info->clk_rate[0][0] : 0);
 			for (j = 0; j < soc_info->num_clk; j++) {
 				rc = cam_soc_util_clk_enable(soc_info->clk[j],
 					soc_info->clk_name[j],
@@ -2177,6 +2190,8 @@ int cam_sensor_core_power_up(struct cam_sensor_power_ctrl_t *ctrl,
 				if (rc)
 					break;
 			}
+			pr_emerg("vamos-cam-sensor: mclk enable clocks end rc=%d\n",
+				rc);
 
 			if (rc < 0) {
 				CAM_ERR(CAM_SENSOR, "clk enable failed");
@@ -2195,6 +2210,11 @@ int cam_sensor_core_power_up(struct cam_sensor_power_ctrl_t *ctrl,
 				CAM_ERR(CAM_SENSOR, "Invalid gpio_num_info");
 				goto power_up_failed;
 			}
+			pr_emerg("vamos-cam-sensor: gpio seq=%d valid=%u gpio=%u val=%ld no_gpio=%d\n",
+				power_setting->seq_type,
+				gpio_num_info->valid[power_setting->seq_type],
+				gpio_num_info->gpio_num[power_setting->seq_type],
+				power_setting->config_val, no_gpio);
 			CAM_DBG(CAM_SENSOR, "gpio set val %d",
 				gpio_num_info->gpio_num
 				[power_setting->seq_type]);
@@ -2217,6 +2237,14 @@ int cam_sensor_core_power_up(struct cam_sensor_power_ctrl_t *ctrl,
 		case SENSOR_VAF_PWDM:
 		case SENSOR_CUSTOM_REG1:
 		case SENSOR_CUSTOM_REG2:
+			pr_emerg("vamos-cam-sensor: regulator seq=%d seq_val=%u num_vreg=%d name=%s config=%ld\n",
+				power_setting->seq_type,
+				power_setting->seq_val, num_vreg,
+				(power_setting->seq_val < num_vreg &&
+				 soc_info->rgltr_name[power_setting->seq_val]) ?
+					soc_info->rgltr_name[power_setting->seq_val] :
+					"<invalid>",
+				power_setting->config_val);
 			if (power_setting->seq_val == INVALID_VREG)
 				break;
 
@@ -2243,6 +2271,11 @@ int cam_sensor_core_power_up(struct cam_sensor_power_ctrl_t *ctrl,
 					goto power_up_failed;
 				}
 
+				pr_emerg("vamos-cam-sensor: regulator enable begin name=%s min=%d max=%d load=%d\n",
+					soc_info->rgltr_name[vreg_idx],
+					soc_info->rgltr_min_volt[vreg_idx],
+					soc_info->rgltr_max_volt[vreg_idx],
+					soc_info->rgltr_op_mode[vreg_idx]);
 				rc =  cam_soc_util_regulator_enable(
 					soc_info->rgltr[vreg_idx],
 					soc_info->rgltr_name[vreg_idx],
@@ -2256,6 +2289,8 @@ int cam_sensor_core_power_up(struct cam_sensor_power_ctrl_t *ctrl,
 						soc_info->rgltr_name[vreg_idx]);
 					goto power_up_failed;
 				}
+				pr_emerg("vamos-cam-sensor: regulator enable end name=%s rc=%d\n",
+					soc_info->rgltr_name[vreg_idx], rc);
 				power_setting->data[0] =
 						soc_info->rgltr[vreg_idx];
 			} else {
@@ -2263,7 +2298,11 @@ int cam_sensor_core_power_up(struct cam_sensor_power_ctrl_t *ctrl,
 					power_setting->seq_val, num_vreg);
 			}
 
+			pr_emerg("vamos-cam-sensor: power domain enable begin dev=%s\n",
+				dev_name(soc_info->dev));
 			rc = cam_soc_util_power_domain_enable_default(soc_info);
+			pr_emerg("vamos-cam-sensor: power domain enable end rc=%d\n",
+				rc);
 			if (rc < 0) {
 				CAM_ERR(CAM_SENSOR,
 					"Power domain enable failed with rc = %d", rc);
@@ -2272,6 +2311,8 @@ int cam_sensor_core_power_up(struct cam_sensor_power_ctrl_t *ctrl,
 			rc = msm_cam_sensor_handle_reg_gpio(
 				power_setting->seq_type,
 				gpio_num_info, 1);
+			pr_emerg("vamos-cam-sensor: regulator gpio assert seq=%d rc=%d\n",
+				power_setting->seq_type, rc);
 			if (rc < 0) {
 				CAM_ERR(CAM_SENSOR,
 					"Error in handling VREG GPIO");
diff --git a/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_cci/cam_cci_core.c b/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_cci/cam_cci_core.c
index d812854..5d2c0aa 100644
--- a/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_cci/cam_cci_core.c
+++ b/drivers/media/platform/msm/camera/camera_kt/drivers/cam_sensor_module/cam_cci/cam_cci_core.c
@@ -1261,6 +1261,12 @@ static int32_t cam_cci_read(struct v4l2_subdev *sd,
 	soc_info = &cci_dev->soc_info;
 	base = soc_info->reg_map[0].mem_base;
 
+	pr_emerg("vamos-cam-cci: read begin cci=%d master=%d queue=%d sid=0x%x slave=0x%x retries=%u id_map=%u freq=%u addr=0x%x addr_type=%u data_type=%u num_byte=%u\n",
+		cci_dev->soc_info.index, master, queue, c_ctrl->cci_info->sid,
+		c_ctrl->cci_info->sid << 1, c_ctrl->cci_info->retries,
+		c_ctrl->cci_info->id_map, c_ctrl->cci_info->i2c_freq_mode,
+		read_cfg->addr, read_cfg->addr_type, read_cfg->data_type,
+		read_cfg->num_byte);
 	/*
 	 * Call validate queue to make sure queue is empty before starting.
 	 * If this call fails, don't proceed with i2c_read call. This is to
@@ -1292,6 +1298,8 @@ static int32_t cam_cci_read(struct v4l2_subdev *sd,
 		c_ctrl->cci_info->id_map << 18;
 	rc = cam_cci_write_i2c_queue(cci_dev, val, master, queue);
 	if (rc < 0) {
+		pr_emerg("vamos-cam-cci: write set-param failed val=0x%x rc=%d\n",
+			val, rc);
 		CAM_DBG(CAM_CCI,
 			"Failed to write param_cmd for cci: %d, master: %d, queue: %d, rc: %d",
 			cci_dev->soc_info.index, master, queue, rc);
@@ -1317,6 +1325,8 @@ static int32_t cam_cci_read(struct v4l2_subdev *sd,
 		val |= ((read_cfg->addr >> (i << 3)) & 0xFF)  <<
 		((read_cfg->addr_type - i) << 3);
 	}
+	pr_emerg("vamos-cam-cci: addr-cmd val=0x%x addr=0x%x addr_type=%u\n",
+		val, read_cfg->addr, read_cfg->addr_type);
 
 	rc = cam_cci_write_i2c_queue(cci_dev, val, master, queue);
 	if (rc < 0) {
@@ -1346,6 +1356,9 @@ static int32_t cam_cci_read(struct v4l2_subdev *sd,
 	val = cam_io_r_mb(base + CCI_I2C_M0_Q0_CUR_WORD_CNT_ADDR
 			+ master * 0x200 + queue * 0x100);
 	CAM_DBG(CAM_CCI, "cur word cnt 0x%x", val);
+	pr_emerg("vamos-cam-cci: execute cur_word_cnt=0x%x start_mask=0x%x exp_words=%u\n",
+		val, 1 << ((master * 2) + queue),
+		((read_cfg->num_byte / 4) + 1));
 	cam_io_w_mb(val, base + CCI_I2C_M0_Q0_EXEC_WORD_CNT_ADDR
 			+ master * 0x200 + queue * 0x100);
 
@@ -1372,6 +1385,8 @@ static int32_t cam_cci_read(struct v4l2_subdev *sd,
 	}
 
 	if (cci_dev->cci_master_info[master].status) {
+		pr_emerg("vamos-cam-cci: read status error master=%d status=0x%x slave=0x%x\n",
+			master, cci_dev->cci_master_info[master].status, c_ctrl->cci_info->sid << 1);
 		CAM_ERR(CAM_CCI, "ERROR with Slave 0x%x:",
 			(c_ctrl->cci_info->sid << 1));
 		rc = -EINVAL;
