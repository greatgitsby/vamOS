diff --git a/drivers/media/platform/msm/camera/cam_sensor_module/cam_cci/cam_cci_core.c b/drivers/media/platform/msm/camera/cam_sensor_module/cam_cci/cam_cci_core.c
index f859484b..2a65926f 100644
--- a/drivers/media/platform/msm/camera/cam_sensor_module/cam_cci/cam_cci_core.c
+++ b/drivers/media/platform/msm/camera/cam_sensor_module/cam_cci/cam_cci_core.c
@@ -614,6 +614,16 @@ static int32_t cam_cci_set_clk_param(struct cci_device *cci_dev,
 	}
 	cci_dev->i2c_freq_mode[master] = i2c_freq_mode;
 
+	pr_emerg("vamos-legacy-cci: set_clk_param master=%d freq_mode=%d scl_ctl=0x%08x sda0=0x%08x sda1=0x%08x sda2=0x%08x misc=0x%08x thigh=%u tlow=%u stretch=%u trdhld=%u tsp=%u\n",
+		master, i2c_freq_mode,
+		clk_params->hw_thigh << 16 | clk_params->hw_tlow,
+		clk_params->hw_tsu_sto << 16 | clk_params->hw_tsu_sta,
+		clk_params->hw_thd_dat << 16 | clk_params->hw_thd_sta,
+		clk_params->hw_tbuf,
+		clk_params->hw_scl_stretch_en << 8 | clk_params->hw_trdhld << 4 | clk_params->hw_tsp,
+		clk_params->hw_thigh, clk_params->hw_tlow,
+		clk_params->hw_scl_stretch_en, clk_params->hw_trdhld, clk_params->hw_tsp);
+
 	return 0;
 }
 
@@ -1170,6 +1180,12 @@ static int32_t cam_cci_read(struct v4l2_subdev *sd,
 	val = CCI_I2C_SET_PARAM_CMD | c_ctrl->cci_info->sid << 4 |
 		c_ctrl->cci_info->retries << 16 |
 		c_ctrl->cci_info->id_map << 18;
+	pr_emerg("vamos-legacy-cci: read master=%d queue=%d sid=0x%x retries=%u id_map=%u freq_mode=%d set_param=0x%08x addr=0x%x addr_type=%u num_byte=%u scl_ctl=0x%08x misc_ctl=0x%08x\n",
+		master, queue, c_ctrl->cci_info->sid, c_ctrl->cci_info->retries,
+		c_ctrl->cci_info->id_map, c_ctrl->cci_info->i2c_freq_mode, val,
+		read_cfg->addr, read_cfg->addr_type, read_cfg->num_byte,
+		cam_io_r_mb(base + CCI_I2C_M0_SCL_CTL_ADDR + master * 0x100),
+		cam_io_r_mb(base + CCI_I2C_M0_MISC_CTL_ADDR + master * 0x100));
 	rc = cam_cci_write_i2c_queue(cci_dev, val, master, queue);
 	if (rc < 0) {
 		CAM_DBG(CAM_CCI, "failed rc: %d", rc);
@@ -1222,6 +1238,12 @@ static int32_t cam_cci_read(struct v4l2_subdev *sd,
 	cam_io_w_mb(val, base + CCI_I2C_M0_Q0_EXEC_WORD_CNT_ADDR
 			+ master * 0x200 + queue * 0x100);
 
+	pr_emerg("vamos-legacy-cci: queue-ready master=%d queue=%d cur_word_cnt=%u qstart=0x%x scl_ctl=0x%08x misc_ctl=0x%08x exp_words=%d\n",
+		master, queue, val, 1 << ((master * 2) + queue),
+		cam_io_r_mb(base + CCI_I2C_M0_SCL_CTL_ADDR + master * 0x100),
+		cam_io_r_mb(base + CCI_I2C_M0_MISC_CTL_ADDR + master * 0x100),
+		((read_cfg->num_byte / 4) + 1));
+
 	val = 1 << ((master * 2) + queue);
 	cam_io_w_mb(val, base + CCI_QUEUE_START_ADDR);
 	CAM_DBG(CAM_CCI,
@@ -1249,6 +1271,11 @@ static int32_t cam_cci_read(struct v4l2_subdev *sd,
 	read_words = cam_io_r_mb(base +
 		CCI_I2C_M0_READ_BUF_LEVEL_ADDR + master * 0x100);
 	exp_words = ((read_cfg->num_byte / 4) + 1);
+	/* Do NOT read READ_DATA here: it pops the FIFO and would corrupt the
+	 * data-extraction loop below. Only log the non-destructive buf level.
+	 */
+	pr_emerg("vamos-legacy-cci: read-done master=%d sid=0x%x read_words=%d exp_words=%d\n",
+		master, c_ctrl->cci_info->sid, read_words, exp_words);
 	if (read_words != exp_words) {
 		CAM_ERR(CAM_CCI, "read_words = %d, exp words = %d",
 			read_words, exp_words);
diff --git a/drivers/media/platform/msm/camera/cam_sensor_module/cam_cci/cam_cci_dev.c b/drivers/media/platform/msm/camera/cam_sensor_module/cam_cci/cam_cci_dev.c
index 958737f1..7655aefc 100644
--- a/drivers/media/platform/msm/camera/cam_sensor_module/cam_cci/cam_cci_dev.c
+++ b/drivers/media/platform/msm/camera/cam_sensor_module/cam_cci/cam_cci_dev.c
@@ -70,6 +70,10 @@ irqreturn_t cam_cci_irq(int irq_num, void *data)
 	irq_status0 = cam_io_r_mb(base + CCI_IRQ_STATUS_0_ADDR);
 	irq_status1 = cam_io_r_mb(base + CCI_IRQ_STATUS_1_ADDR);
 	CAM_DBG(CAM_CCI, "irq0:%x irq1:%x", irq_status0, irq_status1);
+	pr_emerg("vamos-legacy-cci: irq status0=0x%08x status1=0x%08x m0_rd_buf=0x%x m1_rd_buf=0x%x\n",
+		irq_status0, irq_status1,
+		cam_io_r_mb(base + CCI_I2C_M0_READ_BUF_LEVEL_ADDR),
+		cam_io_r_mb(base + CCI_I2C_M0_READ_BUF_LEVEL_ADDR + 0x100));
 
 	if (irq_status0 & CCI_IRQ_STATUS_0_RST_DONE_ACK_BMSK) {
 		if (cci_dev->cci_master_info[MASTER_0].reset_pending == TRUE) {
